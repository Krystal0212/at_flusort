name: at_flusort
description: "A flexible sorting algorithm package for Flutter and Dart."
version: 0.0.1
homepage: https://github.com/Krystal0212/at_flusort

environment:
  sdk: ^3.1.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: